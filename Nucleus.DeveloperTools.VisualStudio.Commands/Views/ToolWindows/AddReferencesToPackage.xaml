<platform:DialogWindow  
  x:Class="Nucleus.DeveloperTools.VisualStudio.Commands.Views.AddReferencesToPackage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:platform="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
  xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
  toolkit:Themes.UseVsTheme="True"
  mc:Ignorable="d"
  Width="600"
  Height="400"
  xmlns:viewmodels="clr-namespace:Nucleus.DeveloperTools.VisualStudio.Commands.ViewModels"
  d:DataContext="{d:DesignInstance viewmodels:AddReferencesToPackage}"
  d:DesignHeight="600"
  d:DesignWidth="500"
  Title="Add References to package.xml">
  <!--https://stackoverflow.com/questions/3138628/grid-lines-in-wpf-listview-->
  

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Label Content="There are no references to add."  Visibility="{Binding IsWarningVisible}" />

    <ListView 
      ItemsSource="{Binding NewReferences}" 
      Padding="8" 
      Visibility="{Binding IsListVisible}">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="">
            <GridViewColumn.CellTemplate>
              <DataTemplate>
                <CheckBox IsChecked="{Binding IsSelected}" />
              </DataTemplate>
            </GridViewColumn.CellTemplate>
          </GridViewColumn>
          <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" Width="400" />
          <GridViewColumn DisplayMemberBinding="{Binding Version}" Header="Version" Width="100" />
        </GridView>
      </ListView.View>
    </ListView>
    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="8" HorizontalAlignment="Right" >
      <Button Content="OK" Click="OK_Clicked" Margin="0,0,8,8" />
      <Button Content="Cancel" Click="Cancel_Clicked" Margin="0,0,8,8" />
    </StackPanel>
  </Grid>
</platform:DialogWindow>
